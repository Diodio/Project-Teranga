ALTER TABLE utilisateur ADD connected VARCHAR(60) DEFAULT NULL, ADD connectedDate DATETIME DEFAULT NULL, ADD disconnectedDate DATETIME DEFAULT NULL;

Updating database schema...
Database schema updated successfully! "1" queries were executed
